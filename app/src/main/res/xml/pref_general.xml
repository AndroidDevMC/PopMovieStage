<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <ListPreference
        android:key="@string/sortKey"
        android:title="@string/sortTitle"
        android:defaultValue="@string/defaultSortOpt"
        android:entries="@array/sortOpt"
        android:entryValues="@array/sortOptValues"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
